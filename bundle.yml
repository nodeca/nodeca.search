bundles:
  frontend:
    - search


packages:
  admin:
    entries:
      - client/admin
      - server/admin

  search:
    depends:
      - common

    entries:
      - client/search
      - server/search
      - internal/search
