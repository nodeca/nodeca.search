bundles:
  frontend:
    - search


packages:
  admin:
    entries:
      - client/admin
      - server/admin

  common:
    entries:
      - client/common

  search:
    depends:
      - common

    entries:
      - client/search
      - server/search
      - internal/search
